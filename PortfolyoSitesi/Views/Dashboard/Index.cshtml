@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/AdminLayout/Index.cshtml";
}

<!-- Scrollable Dashboard Content -->
<div class="flex-1 overflow-y-auto bg-background-light dark:bg-background-dark p-6 md:p-10">
    <div class="mx-auto max-w-7xl">

        <!-- Title Section -->
        <div class="mb-8 flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
            <div>
                <h3 class="text-2xl font-bold text-slate-800 dark:text-white">Dashboard</h3>
                <p class="text-sm text-slate-500 dark:text-slate-400">
                    Real-time data updates for your platform.
                </p>
            </div>
        </div>

        <!-- Cards -->
        <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">

            <!-- Card: Projects -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    Projects Count : @ViewBag.ProjectsCount
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">folder</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.LastProject</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Most Recent Project</p>
                </div>
            </div>

            <!-- Card: Category -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    @ViewBag.MostCategoryName Category Projects Count : @ViewBag.MostCategoryCount
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">category</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.MostCategoryName</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Category with the Most Projects</p>
                </div>
            </div>

            <!-- Card: Top Skill -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    @ViewBag.TopSkillName Level : @ViewBag.TopSkillLevel
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">workspace_premium</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.TopSkillName</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Highest Level Skill</p>
                </div>
            </div>

            <!-- Card: Services -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    Services Count : @ViewBag.ServicesCount
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">design_services</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.LastService</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Most Recent Service</p>
                </div>
            </div>

            <!-- Card: Testimonials -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    Testimonials Count : @ViewBag.TestimonialsCount
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">reviews</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.LastTestimonial</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Most Recent Testimonial</p>
                </div>
            </div>

            <!-- Card: Messages -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <span class="absolute top-4 right-4 rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">
                    Messages Count : @ViewBag.MessagesCount
                </span>
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">chat</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.LastMessageSender</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Last Message Sender</p>
                </div>
            </div>

            <!-- Card: First Experience -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">work_history</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.FirstExperienceTitle</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">First Experience</p>
                </div>
            </div>

            <!-- Card: First Project -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">rocket_launch</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.FirstProjectName</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">First Project</p>
                </div>
            </div>

            <!-- Card: Last Skill -->
            <div class="group relative flex flex-col justify-between rounded-xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-surface-dark p-6 shadow-sm hover:shadow-md transition-shadow">
                <div class="flex items-start justify-between">
                    <div class="rounded-lg bg-primary/10 p-3 text-primary">
                        <span class="material-symbols-outlined text-3xl">psychology</span>
                    </div>
                </div>
                <div class="mt-4">
                    <h4 class="text-3xl font-extrabold text-slate-900 dark:text-white">@ViewBag.LastSkill</h4>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Most Recent Skill</p>
                </div>
            </div>

        </div>
    </div>
</div>
